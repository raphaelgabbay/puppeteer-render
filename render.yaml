services:
  - type: web
    name: flood-automation
    env: node
    buildCommand: |
      npm install
      npm run build
    startCommand: npm start
    envVars:
      - key: PUPPETEER_SKIP_CHROMIUM_DOWNLOAD
        value: false
